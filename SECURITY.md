# Security

- Report vulnerabilities to security@hunter-seeker.net. Expect an acknowledgement within 2 business days.
- Scope: anything that makes `verify` return `valid` for a payload the engine did not sign, or
  leaks information beyond the four statuses.
- Out of scope: the correctness of a decision inside a valid Verdict (see the product's trust block).
